# T6 證據 — 授權中介層（角色 + 列級 scope + 欄位遮罩）

- 日期：2026-07-15｜實作：Codex｜驗收：Fable 5 親跑
- 產物：`api/app/authz/{dependencies,scopes,fields}.py`、`tests/test_authz.py`

## 驗收（親跑 unittest）
| 測試 | 結果 |
|---|---|
| admin scope_predicate=None，看到全部 room | ✅ |
| **site-scoped staff 只看到自己案場的 room/rent_confirm（負向，Codex必測①）** | ✅ |
| staff 的 allowed_columns 排除 tenant_contract 敏感欄(電話/租金) | ✅ |
| require_roles 擋錯誤角色回 403 | ✅ |
- 全後端測試合跑：test_authz+test_meta+test_auth+test_schema_models = **18/18 OK**

## site 連結登記（scope 依案場）
- site=id｜room=site_id｜tenant_contract/rent_confirm/room_fixed_fee/exception_charge=room_id→room.site_id｜meter_reading=assignment_id→room_meter_assignment.room_id→room.site_id｜mgmt_reminder=nullable site_id｜meter/avg_price=site-agnostic→scoped 非 admin **安全 deny(false())**

## httpx2 事件（更正一次誤判，誠實記錄）
- 現象：TestClient 出現 `install httpx2 instead` 警告；我初判「Codex 竄改了 venv 的 starlette」。
- 查證：force-reinstall starlette **1.3.1** 從 PyPI 乾淨重裝後仍是 `import httpx2 as httpx`；`pip install httpx2` **成功**(httpx2 2.6.0 為真實套件)。→ **結論更正：Codex 並未竄改函式庫**，`import httpx2` 是 starlette 1.3.1 正牌相依；Codex 先前寫的 httpx2.py 只是沙箱無網路的克難替代(已刪)。
- 處置：裝真 httpx2、requirements 測試依賴改 httpx2 → 警告消失、18 測全過。
- 殘留：psycopg ResourceWarning(測試未關連線)，小疵、非功能問題，後續測試可補 with/close。
