# T18 — 電費發布→應收

實作：Codex（b746w2por），指揮方親驗。

## 交付
- `api/app/services/billing.py`：approve/publish/reverse 服務；publish 組 rent_confirm（房租 from tenant_contract + 電費 from billing_run_detail + 固定費 Σroom_fixed_fee + 例外 Σexception_charge → total）；唯一鍵 (room_id,billing_ym,charge_type,run_version) IntegrityError→409；reverse 產負額沖銷列不刪原；審計。
- `api/app/routers/billing.py`：POST /runs/{id}/approve、/publish、/reverse（admin|manager）。
- `api/tests/test_publish.py`：組成金額、重複 publish 409、非 approved 409、reverse 反向、staff 403、審計。

## 親驗
- `python -m unittest discover -s tests` → **53 tests OK**。
- Live 金流（room_id=4, 202606）：建 run→calculated→approve→approved→publish→published，rent_confirm 產生（charge_type=月結、status=已確認、電費 1635、total 18635）與匯入歷史列並存；reverse→reversed；**重複 publish→409（必測②）**。
- 往後權威策略：publish 用新 charge_type/version，不覆寫匯入歷史列。

## 事件（誠實記錄）
- Live 測試後清理 SQL 用 `run_version >= 測試版本` 條件過寬，誤刪 room4/202606 的 2 筆匯入歷史 rent_confirm（總數 1898→1896）。已用 `python -m app.importer.cli` 重新匯入還原（truncate+reload，回 1898）。教訓：清理用精確 id/run_id 條件，勿用 >=。

狀態：done。
