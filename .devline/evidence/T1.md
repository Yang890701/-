# T1 證據 — monorepo 骨架

- 日期：2026-07-15｜實作：Codex(gpt-5.5)｜驗收：Fable 5 親跑
- 環境：Windows / Node v24.14.1 / Python 3.14.3 / PostgreSQL 17.10（原生，服務 postgresql-x64-17）

## 建立的結構（Codex，read-back 通過）
- `web/`：Next.js 15 + TS + @tanstack/react-table + xlsx(SheetJS)，`/` 與 `/login` 佔位頁
- `api/`：FastAPI `/health`；`app/{main,config,routers,db,storage}`；`storage/{base,local}`（本地資料夾 adapter）；alembic（`env.py` 讀 `DATABASE_URL`，無硬編碼帳密）
- `infra/docker-compose.yml`：postgres17+minio（保留供機器 A）
- `.env.example`、`README.md`

## 驗收（可執行指令＋實際結果）
| 驗收項 | 指令 | 結果 |
|---|---|---|
| 前端 build 綠 | `npm --prefix web run build` | ✅ Next.js 15.5.20 `Compiled successfully in 3.2s`；`/`,`/login`,`/_not-found` 靜態生成、型別檢查通過 |
| 依賴可裝(py3.14) | `pip install -r requirements.txt`(.venv) | ✅ fastapi-0.139.0 / sqlalchemy-2.0.51 / psycopg-3.3.4 / pydantic-2.13.4 / alembic-1.18.5 / argon2-cffi / pyjwt / uvicorn 等全裝 |
| PG 連得上 | `alembic upgrade head`（DATABASE_URL=haoshi_dev） | ✅ `Context impl PostgresqlImpl`（連上，尚無版本→no-op，符合預期） |
| /health 200 | `uvicorn app.main:app` + `curl /health` | ✅ `{"status":"ok"}` |

## DB
- `haoshi_dev` 資料庫 + `haoshi` 角色（dev 密碼）建立、登入驗證通過。
- ⚠ 超級使用者 `postgres` 密碼為 winget 靜默安裝之預設 `postgres`，僅本機 dev；建議上線前/對外前更改。

## 刻意延後（Codex）
schema/migrations/auth/元數據 allowlist/真實 UI/lint/物件儲存 S3 實作/業務邏輯 → T2/T3+。
