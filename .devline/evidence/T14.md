# T14 證據 — 審計檢視頁（前端）

- 日期：2026-07-15｜實作：Codex｜驗收：Fable 5 親跑 build
- 產物：`web/app/audit/page.tsx`（管理者專屬）、`web/app/providers.tsx`（header 加 admin-only /audit 連結）、`web/app/globals.css`（樣式）

## 驗收
- `npm run build` ✅ 通過，新增 `/audit` 路由（3.69kB）；/、/login、/audit 皆生成。
- 頁面：admin-only 守衛；篩選（action 類型／日期範圍）＋分頁；表格顯示 時間/使用者/動作/表/筆數/篩選；呼叫後端 GET /api/audit（由 T9 提供）。
- 樣式沿用無 AI 感 baseline（米色/白卡/細邊框/思源黑體）。
- 註：與後端 /api/audit 的整合待後端 T9 完成後一併 live 驗。
