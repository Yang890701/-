# T15 — 主檔 CRUD（site/meter/room）

實作：Codex（b80ew6daq），指揮方親驗。

## 改動
- 後端 `api/app/routers/master.py`（新 router）+ `main.py` 註冊。
  - POST/PUT/DELETE(軟刪)/GET(list, include_inactive) for site|meter|room。
  - 授權：create/update/delete 限 admin/manager（403 否則）。
  - FK 校驗：room.site_id / room.meter_id 須存在（400 否則）。
  - 自然鍵重複：active 預檢 + DB partial-unique catch → 409。
  - 每次異動寫 AuditLog（master_create/update/delete）。
- 測試 `api/tests/test_master.py`。
- 前端 `web/app/master/{site,meter,room}/page.tsx` + 共用 `_components/master-page.tsx`；`portal-data.ts` 三連結轉 available；`globals.css` 樣式。

## 親驗
- 後端：`python -m unittest discover -s tests` → **35 tests OK**（含 master：建立/壞FK/重複鍵/軟刪歷史/staff 403/審計）。
- 前端：`npm run build` 綠，含 `/master/site`、`/master/meter`、`/master/room`。
- Live（重啟後端載入 master 路由後）：建立案場✓；重複自然鍵→409✓；房號指不存在案場→400✓；軟刪後預設列表 125 筆排除、`?include_inactive=true` 126 筆含歷史（deleted_at 有值）✓。
- 測試殘留 ZZTEST 案場已清除。

狀態：done。備註：ruff 未在 venv（lint 稍後補）；表級寫入用角色閘（admin/manager），未加 table_meta create/update/delete_roles 欄（避免動 schema）。
