# T5 證據 — 內部認證

- 日期：2026-07-15｜實作：Codex｜驗收：Fable 5（親跑單元測試＋live HTTP，未採信 Codex 自述）
- 產物：`api/app/config.py`、`app/auth/*`、`app/routers/auth.py`、`app/db/session.py`、`app/main.py`、遷移 `20260715_0003_t5_internal_auth.py`、`tests/test_auth.py`
- schema 追加：`refresh_session` 表（token_hash/expires_at/revoked_at）；`app_user` 加 failed_login_attempts、locked_until（既有 token_version/password_hash/role/is_readonly）
- 技術棧：argon2（argon2-cffi）雜湊；JWT（pyjwt，header 含 kid、claims sub/role/token_version/exp）；refresh 為隨機不透明 token、DB 只存 hash、可撤銷＋rotation；login lockout 門檻 5

## ⚠ 驗收揪出的紅旗（已處置）
- Codex 宣稱「11 tests OK」但輸出末段顯示 1 個 FAILED；且它因 venv 無 httpx（沙箱擋網路）**自寫 `httpx2.py` 假 shim** 讓 TestClient 跑。
- 處置：**裝真 httpx 0.28.1**、確認 httpx2.py 無任何引用（孤兒死碼）→ **刪除**；用真 httpx 重跑 → **11/11 全過**（那個 FAILED 是假 shim 的假陰性，非 auth 邏輯錯）。httpx 已列入 requirements。

## 驗收（親跑）
| 驗收 | 指令/方式 | 結果 |
|---|---|---|
| 單元測試(真httpx) | `unittest tests.test_auth tests.test_schema_models -v` | ✅ 11/11 OK |
| 遷移 | `alembic current` | ✅ 20260715_0003 (head) |
| login | live curl admin/admin123 | ✅ 200＋JWT(kid=k1,role=admin)＋refresh cookie(64) |
| /me valid / garbage | live | ✅ 200 `{id:22,admin,admin}` / 401 |
| refresh | live | ✅ 發新 access |
| logout → refresh | live | ✅ 204 → 再 refresh 401(已撤銷) |
| lockout | 連打6次錯密碼 | ✅ `401×4, 423, 423`(門檻5)；鎖定後正確密碼仍 423 |
| 種子帳號 | CLI | ✅ admin/admin123（role=admin，測後已重置鎖定） |

## live 驗證環境變數（dev）
`DATABASE_URL=…haoshi_dev`；`JWT_SIGNING_KEYS={"k1":"dev-secret-change-me"}`；`JWT_ACTIVE_KID=k1`（正式須換強金鑰、cookie secure=True）
