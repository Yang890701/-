# T16 — 房↔電表關聯 + 度數/電價上傳

實作：Codex（b0u8ygjkx），指揮方親驗。

## 改動
- 後端 `api/app/routers/meter.py`（新）+ main.py 註冊：
  - POST/GET /api/meter-assignments（建立/列出；FK 校驗；區間重疊→409）
  - POST /api/meter-assignments/{id}/change-meter（換表：關閉現關聯+建相鄰新關聯+meter_event）
  - POST/GET /api/meter-readings（reading_kind；**須先傳前期**→409；缺值/異常→reading_exception 佇列）
  - GET /api/reading-exceptions?status=open
  - POST/GET /api/avg-prices（重複 meter_id+billing_ym→409）
  - 皆寫審計。
- 測試 `api/tests/test_meter.py`。
- 前端 `web/app/upload/reading/page.tsx`、`web/app/upload/price/page.tsx`；portal-data 兩連結轉 available；globals.css 成功/警告樣式。

## 親驗
- exclusion 約束 `room_meter_assignment_no_overlap`（contype=x）**確認存在於 DB**（T3 migration 就有）。
- 後端 `python -m unittest discover -s tests` → **42 tests OK**（含必測③區間重疊 409、須先傳前期、換表、缺值→exception、電價重複 409、審計）。
- 前端 `npm run build` 綠，含 `/upload/reading`、`/upload/price`。
- Live（重啟後端載入 meter 路由後）：GET meter-assignments/reading-exceptions/avg-prices 皆掛載且回真資料。

## 備註
- 景平 401-404：資料可表示（多 assignment 共用 meter_id + meter_category 標記），readings 可入；合併計費留 T17。
- attachment 於本任務為選填（未接私有 bucket 上傳流程）。
- ruff 未在 venv（lint 稍後補）。

狀態：done。
