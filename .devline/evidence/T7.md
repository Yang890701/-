# T7 證據 — 元數據層（resolver + seed + governance + meta 端點）

- 日期：2026-07-15｜實作：Codex｜驗收：Fable 5 親跑
- 產物：`api/app/meta/{resolver,seed,governance}.py`、`app/routers/meta.py`、`MetadataChangeLog` 模型、遷移 `20260715_0004`、`tests/test_meta.py`

## 驗收（親跑）
| 項 | 結果 |
|---|---|
| tests.test_meta | ✅ 3/3 OK（resolver 拒未登錄表/欄/運算子；staff 看不到敏感欄；seed 冪等） |
| alembic current | ✅ 20260715_0004 (head) |
| seed | ✅ table_meta=10、column_meta=56 |
| column_meta 結構 | ✅ table_code/col_code/label/type/physical_column/filterable/operators/read_roles/write_roles/filter_roles/sort_roles/export_roles |
| 已註冊表 | site/meter/room/tenant_contract/rent_confirm/meter_reading/avg_price/room_fixed_fee/exception_charge/mgmt_reminder |

## 角色遮罩（親查 column_meta，核心安全點）
- `tenant_contract.contact_phone`、`tenant_contract.rent`：read/export = {admin,manager,accounting} → **staff 排除** ✅
- `rent_confirm.{rent,electricity,fixed_fee,exception,total}_amount`：read = {admin,manager,accounting} → **staff 排除** ✅
- 一般欄 `room.room_code`/`site.site_code`：read = {admin,manager,accounting,staff,readonly}（全角色）✅
- tenant_contract：staff 可見 6 欄 / admin 8 欄（2 敏感欄對 staff 隱藏）

## 安全設計（憲法原則2/3）
- resolver 僅透過 table_meta/column_meta allowlist 把 public code → 受控 physical，**不接受任意字串、不組 raw SQL**；每欄運算子白名單。
- 此次 Codex **未再造 httpx shim**（用已裝的真 httpx TestClient）。
- 未涵蓋：resolver 的「活體」查詢負向測試留待 T8（查詢端點建好後一併 live 驗）。
