# T11–T13 證據 — DMO 前端（登入 + 通用檢視 + 篩選 + 匯出）

- 日期：2026-07-15｜實作：Codex（前端）+ Fable 5（親驗＋補 CORS）｜此檔涵蓋 T11/T12/T13
- 產物：`web/lib/api.ts`、`web/app/{providers,layout}.tsx`、`web/app/login/page.tsx`、`web/app/page.tsx`（通用檢視頁）、`web/app/globals.css`

## 驗收（親跑）
| 項 | 結果 |
|---|---|
| 前端 `npm run build` | ✅ 編譯成功（`/` 16kB、`/login` 2.37kB；僅 1 個 autoprefixer 無害警告）。**注意：Codex 沙箱跑不了 build（卡住+殭屍node），此 build 由我清 node/.next 後親跑通過** |
| tsc --noEmit | ✅ 型別乾淨 |
| 前後端服務 | ✅ backend :8000 /health ok、frontend :3000 /login & / 皆 200（Next 頁、/_next/static bundle） |
| **CORS（我補的修正）** | ✅ Codex 漏掛 CORSMiddleware→瀏覽器會被擋；我在 main.py 補上（allow_origins=cors_allowlist, allow_credentials）→預檢回 200+Access-Control-Allow-Origin: localhost:3000 |
| 後端流程(前端會打的) | ✅ login→/api/meta/tables 回 10 表中文標籤→/api/data/query 分頁→/export xlsx（T8 已 live 驗：679=679、中文標題、寫審計） |

## 前端設計（無 AI 感）
- 底 #f6f4f0 + 白卡 + 細邊框 #e5e2dc + 淺陰影 + 思源黑體 + 低彩度主色 #4a5a6a；禁紫藍漸層/發光/emoji 圖示。
- 通用檢視頁：讀 /api/meta 動態渲染（不硬編碼欄位）→表選單→依 column_meta 生篩選器→TanStack Table 伺服器端分頁/排序→匯出鈕下載 xlsx。
- API client：access token 存記憶體（非 localStorage）；401 自動 refresh 一次再重試；credentials:include 帶 refresh cookie。

## 誠實標限
- build/服務/CORS/後端流程 皆已親驗；**互動式 React UI（實際點篩選、看表格渲染、按匯出下載）尚未用瀏覽器 click-through 驗證**（本環境無瀏覽器自動化）。使用者於瀏覽器開啟確認為最後一哩。
- 執行需環境變數：後端 `DATABASE_URL / JWT_SIGNING_KEYS / JWT_ACTIVE_KID`；前端 `NEXT_PUBLIC_API_BASE`(預設 localhost:8000)。
