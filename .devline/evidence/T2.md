# T2 證據 — lint/format 基線

- 日期：2026-07-15｜實作：Fable 5（直接做，sonnet 級小任務）
- 產物：`api/ruff.toml`、`web/.prettierrc.json`、`web/.prettierignore`、`web/package.json`(lint/format scripts)

## 驗收（親跑，指令全綠）
| 指令 | 結果 |
|---|---|
| `ruff check .` (api) | ✅ **All checks passed!** |
| `ruff format --check .` (api) | ✅ 39 files already formatted |
| api 後端全測試（確認 ruff 自動改動未破壞） | ✅ 29/29 OK |
| `npm run lint` (web = tsc --noEmit) | ✅ 綠 |
| `npx prettier --check .` (web) | ✅ All matched files use Prettier code style |

## 設定
- api ruff：select E/F/I/W/UP/B；ignore B008(FastAPI Depends 慣例)、E501(行長交 formatter)；排除 alembic/versions、.venv、var。
- web prettier：printWidth 100、雙引號、trailingComma all；.prettierignore 排除 .next/node_modules。
- ruff --fix 已清未用 import、`datetime.timezone.utc`→`UTC`(UP)、統一格式；手動移除 models 未用 `JSON` import。
