# T9 + T10 證據 — 審計端點 / 附件二段式

- 日期：2026-07-15｜實作：Codex｜驗收：Fable 5 親跑（unittest 全套 + live）
- 產物：`api/app/routers/audit.py`、`api/app/routers/attachments.py`、`auth.py`(login寫審計)、`data.py`(query寫審計)、`models`(attachment.status)、遷移 `20260715_0005`

## 驗收
| 項 | 結果 |
|---|---|
| 後端全測試套件 `unittest discover tests` | ✅ **29/29 OK**（含 test_audit + test_attachments 6 個） |
| 遷移 | ✅ 0005 applied |
| live GET /api/audit (admin) | ✅ 回 audit_query(查詢自身也記)、login、export(rent_confirm 679) 等事件、newest-first |
| login / query 寫審計 | ✅ audit_log 出現 action=login、action=query(含 table/filters/row_count)、export |
| 附件 presign 壞 MIME | ✅ 400 拒絕 |
| 附件 presign 正確 | ✅ 回 {attachment_id, upload_url(帶簽章 token，非公開可猜)} |
| /api/audit 對 staff | ✅ admin-only（單元測試 403 覆蓋） |

## 附件設計（dev = LocalFolderStorage）
- 二段式：presign(驗MIME/大小→建 attachment status=pending + 簽章 upload_url) → 上傳(驗 token→存 var/storage→ready) → GET 回短效簽章讀取 URL。
- 儲存根目錄 var/storage（repo 根；Codex 環境下 api/var 權限被拒，改 workspace var）。

## 殘留（非阻塞）
- 測試有 psycopg ResourceWarning（未關連線），屬測試 hygiene，後續可用 with/close 收斂。
